# Example environment variables for the project
# Copy this file to `.env` (root) and fill in real values for local development or for Docker Compose

# Postgres (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=resume_db

# If you run Postgres externally, set a DATABASE_URL instead (example below)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/resume_db

# Server
JWT_SECRET=your_jwt_secret_here
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# LLM / API keys
GEMINI_API_KEY=your_gemini_api_key_here

# Cloudinary (optional)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

